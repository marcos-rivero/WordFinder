# WordFinder

Steps
I supposed I receive a list of strings as a matrix
1- Remove repeated words from wordstream
2- Create a new list of strings taking characters vertically in the same position from horizontal strings.
3- Count match horizontally and vertically and add them to a dictionary
4- Order descending the dictionary and retreiving keys found.
